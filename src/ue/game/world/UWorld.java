package ue.game.world;

public class UWorld extends World
{
	public UWorld(long seed, int size, float water_mass, byte max_level, byte radius, byte drawtype)
	{
		super(seed, size, water_mass, max_level, radius, drawtype);
	}
}
